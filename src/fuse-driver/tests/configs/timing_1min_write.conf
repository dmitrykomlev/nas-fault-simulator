# NAS Emulator FUSE Timing Fault Test Configuration
# Fail write operations after 1 minute of runtime

# Basic Settings
mount_point = ${NAS_MOUNT_POINT}
storage_path = ${NAS_STORAGE_PATH}
log_file = ${NAS_LOG_FILE}
log_level = 3  # DEBUG level for detailed logs

# Fault Injection Master Switch
enable_fault_injection = true

# Timing Fault Configuration - 1 minute threshold on writes
[timing_fault]
enabled = true
after_minutes = 1
operations = write

# Explicitly disable all other fault types
[error_fault]
probability = 0.0     # Disabled

[corruption_fault]
probability = 0.0     # Disabled

[delay_fault]
probability = 0.0     # Disabled

[operation_count_fault]
enabled = false       # Disabled

[partial_fault]
probability = 0.0     # Disabled
